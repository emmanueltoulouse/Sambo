namespace Sambo.Document {
    public class HtmlDocumentConverter : Object, DocumentConverter {
        public PivotDocument to_pivot(string content, string path) {
            // Parse le HTML en AST PivotDocument
            return null; // TODO: Replace with actual PivotDocument instance
        }
        public string from_pivot(PivotDocument pivot) {
            // Génère du HTML à partir de l’AST
            return ""; // TODO: Replace with actual HTML generation logic
        }
    }
}
